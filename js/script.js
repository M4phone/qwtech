
const translations = {
    "en": {
        "nav_home": "Home",
        "nav_about": "About Us",
        "nav_competence": "Competencies",
        "nav_products": "Products",
        "nav_contact": "Contact Us",
        "hero_title": "Trusted Quartz Ware Supplier from Korea",
        "hero_subtitle": "Precision crafting the future. Specialized in Semiconductor Ware Manufacturing & Export.",
        "hero_cta": "Request a Quote",
        "hero_products": "View Products",
        "profile_title": "Company Profile",
        "profile_desc": "QW TECH supplies semiconductor ware and precision quartz machined products based on Korea's advanced semiconductor technology. We are gaining global trust through stable quality and rapid delivery, and we will continue to grow together with our clients.",
        "competence_title": "Core Competencies",
        "comp_1_title": "Easy Quote Request System",
        "comp_1_desc": "Input your specifications and receive a quick and simple quote.",
        "comp_2_title": "Flexible Supply & Fast Delivery",
        "comp_2_desc": "Rapid procurement of raw materials ensuring short lead times.",
        "comp_3_title": "Precision Semiconductor Tech",
        "comp_3_desc": "Manufacturing high-precision machined products with micro-process technology.",
        "comp_4_title": "Korean Advanced Infrastructure",
        "comp_4_desc": "Leveraging a world-class semiconductor industrial ecosystem for quality and productivity.",
        "comp_5_title": "Small Batch & R&D Support",
        "comp_5_desc": "Custom production available from research trials to mass production.",
        "quality_title": "Quality & Certification",
        "quality_desc": "QW-TECH operates a quality management system meeting international export standards, including CoC (Certificate of Conformity).",
        "cert_coc": "CoC Certification",
        "cert_iso": "ISO9001 / ISO14001",
        "cert_rohs": "RoHS Certification",
        "quality_slogan": "\"All semiconductor ware is manufactured under certified quality systems.\"",
        "products_title": "Our Products",
        "products_subtitle": "Click any image to view in detail",
        "tech_title": "Technical & Quality Assurance",
        "tech_desc": "Process equipment <span class='separator'>/</span> Inspection standards <span class='separator'>/</span> Cleaning equipment",
        "tech_slogan": "\"Zero-defect manufacturing\"",
        "global_title": "Global Export Network",
        "global_desc": "QW TECH supplies high-quality wafers to global semiconductor markets in Germany, USA, Japan, etc.",
        "global_slogan": "\"Made in Korea, Trusted Worldwide.\"",
        "contact_title": "Contact Us",
        "contact_desc": "Leave a product inquiry or quote request below. We will reply promptly.",
        "form_name": "Name",
        "form_company": "Company Name",
        "form_email": "Email",
        "form_country": "Country",
        "form_product": "Product Type",
        "form_quantity": "Quantity",
        "form_details": "Request Details (Delivery, Specs, etc.)",
        "form_file": "Attach File (Drawings, Specs)",
        "form_submit": "Request Quote"
    },
    "de": {
        "nav_home": "Startseite",
        "nav_about": "Über Uns",
        "nav_competence": "Kompetenzen",
        "nav_products": "Produkte",
        "nav_contact": "Kontakt",
        "hero_title": "Ihr vertrauenswürdiger Quarzwaren-Lieferant aus Korea",
        "hero_subtitle": "Präzision die Zukunft gestaltet. Spezialisiert auf Halbleiterwarenfertigung & Export.",
        "hero_cta": "Angebot anfordern",
        "hero_products": "Produkte ansehen",
        "profile_title": "Firmenprofil",
        "profile_desc": "QW TECH liefert Halbleiterwaren und präzisionsgefertigte Quarzprodukte basierend auf Koreas fortschrittlicher Halbleitertechnologie. Wir gewinnen weltweites Vertrauen durch stabile Qualität und schnelle Lieferung und wachsen gemeinsam mit unseren Kunden.",
        "competence_title": "Kernkompetenzen",
        "comp_1_title": "Einfaches Angebotssystem",
        "comp_1_desc": "Geben Sie Ihre Spezifikationen ein und erhalten Sie schnell und einfach ein Angebot.",
        "comp_2_title": "Flexible Versorgung & Schnelle Lieferung",
        "comp_2_desc": "Schnelle Beschaffung von Rohstoffen für kurze Lieferzeiten.",
        "comp_3_title": "Präzisions-Halbleitertechnologie",
        "comp_3_desc": "Herstellung hochpräziser Produkte mit Mikroprozesstechnologie.",
        "comp_4_title": "Koreas fortschrittliche Infrastruktur",
        "comp_4_desc": "Nutzung eines erstklassigen Halbleiter-Industrie-Ökosystems für Qualität und Produktivität.",
        "comp_5_title": "Kleinserien & F&E Unterstützung",
        "comp_5_desc": "Maßgeschneiderte Produktion von Forschungsversuchen bis hin zur Massenproduktion.",
        "quality_title": "Qualität & Zertifizierung",
        "quality_desc": "QW-TECH betreibt ein Qualitätsmanagementsystem, das internationale Exportstandards erfüllt, einschließlich CoC (Konformitätsbescheinigung).",
        "cert_coc": "CoC Zertifizierung",
        "cert_iso": "ISO9001 / ISO14001",
        "cert_rohs": "RoHS Zertifizierung",
        "quality_slogan": "\"Alle Halbleiterwaren werden unter zertifizierten Qualitätssystemen hergestellt.\"",
        "products_title": "Unsere Produkte",
        "products_subtitle": "Klicken Sie auf ein Bild für die Detailansicht",
        "tech_title": "Technik & Qualitätssicherung",
        "tech_desc": "Prozessanlagen <span class='separator'>/</span> Prüfstandards <span class='separator'>/</span> Reinigungsanlagen",
        "tech_slogan": "\"Null-Fehler-Fertigung\"",
        "global_title": "Globales Exportnetzwerk",
        "global_desc": "QW TECH liefert hochwertige Wafer an globale Halbleitermärkte in Deutschland, den USA, Japan usw.",
        "global_slogan": "\"Made in Korea, Trusted Worldwide.\"",
        "contact_title": "Kontaktieren Sie uns",
        "contact_desc": "Hinterlassen Sie unten eine Produktanfrage oder ein Angebot. Wir antworten umgehend.",
        "form_name": "Name",
        "form_company": "Firmenname",
        "form_email": "E-Mail",
        "form_country": "Land",
        "form_product": "Produkttyp",
        "form_quantity": "Menge",
        "form_details": "Anfragedetails (Lieferung, Spezifikationen usw.)",
        "form_file": "Datei anhängen (Zeichnungen, Spezifikationen)",
        "form_submit": "Angebot anfordern"
    }
};

// ⚠️ Apps Script 배포 후 아래 URL을 교체하세요
const APPS_SCRIPT_URL = 'https://script.google.com/macros/s/AKfycbxsH6ivf0jkD73V8wnTcFpBkh5TUcV8MC5oQmYGmbOd7j6txHGk4yfNJ72s97hwSepR/exec';

document.addEventListener('DOMContentLoaded', () => {
    const defaultLang = 'en';
    let currentLang = defaultLang;

    const updateLanguage = (lang) => {
        currentLang = lang;
        const elements = document.querySelectorAll('[data-i18n]');

        elements.forEach(element => {
            const key = element.getAttribute('data-i18n');
            if (translations[lang] && translations[lang][key]) {
                if (element.tagName === 'INPUT' || element.tagName === 'TEXTAREA') {
                    // labels are used, not placeholders
                } else {
                    element.innerHTML = translations[lang][key];
                }
            }
        });

        document.querySelectorAll('.lang-btn').forEach(btn => btn.classList.remove('active'));
        document.getElementById(`lang-${lang}`).classList.add('active');
    };

    document.getElementById('lang-en').addEventListener('click', () => updateLanguage('en'));
    document.getElementById('lang-de').addEventListener('click', () => updateLanguage('de'));
    updateLanguage(defaultLang);

    // ── 문의 폼 제출 처리 ──────────────────────────────────────
    const form = document.querySelector('.contact-form');
    if (form) {
        form.addEventListener('submit', async (e) => {
            e.preventDefault();

            const submitBtn = form.querySelector('button[type="submit"]');
            const originalText = submitBtn.textContent;
            submitBtn.disabled = true;
            submitBtn.textContent = 'Sending...';

            try {
                // 파일을 base64로 변환
                const fileInput = document.getElementById('file');
                let fileData = null;
                if (fileInput.files && fileInput.files[0]) {
                    fileData = await readFileAsBase64(fileInput.files[0]);
                }

                const payload = {
                    name:     document.getElementById('name').value.trim(),
                    company:  document.getElementById('company').value.trim(),
                    email:    document.getElementById('email').value.trim(),
                    country:  document.getElementById('country').value.trim(),
                    product:  document.getElementById('product').value.trim(),
                    quantity: document.getElementById('quantity').value.trim(),
                    details:  document.getElementById('details').value.trim(),
                    file:     fileData
                };

                const res = await fetch(APPS_SCRIPT_URL, {
                    method: 'POST',
                    body: JSON.stringify(payload)
                });

                const json = await res.json();

                if (json.success) {
                    showMessage('success', currentLang === 'de'
                        ? 'Ihre Anfrage wurde erfolgreich gesendet!'
                        : 'Your inquiry has been submitted successfully!');
                    form.reset();
                } else {
                    throw new Error(json.error || 'Unknown error');
                }
            } catch (err) {
                console.error(err);
                showMessage('error', currentLang === 'de'
                    ? 'Ein Fehler ist aufgetreten. Bitte versuchen Sie es erneut.'
                    : 'Something went wrong. Please try again.');
            } finally {
                submitBtn.disabled = false;
                submitBtn.textContent = originalText;
            }
        });
    }
});

// 파일을 base64 문자열로 읽기
function readFileAsBase64(file) {
    return new Promise((resolve, reject) => {
        const reader = new FileReader();
        reader.onload = () => {
            // result: "data:mime/type;base64,XXXX..." → base64 부분만 추출
            const base64 = reader.result.split(',')[1];
            resolve({ name: file.name, mimeType: file.type, data: base64 });
        };
        reader.onerror = reject;
        reader.readAsDataURL(file);
    });
}

// ── Hero: Slideshow (mobile) / Mosaic (desktop) ─────────────────────────────
(function () {
    const items = document.querySelectorAll('.hero-mosaic-item');
    const dots  = document.querySelectorAll('.hero-dot');
    if (!items.length) return;

    let current = 0;
    let timer;

    function isMobile() { return window.innerWidth <= 768; }

    function goTo(idx) {
        items[current].classList.remove('active');
        if (dots[current]) dots[current].classList.remove('active');
        current = (idx + items.length) % items.length;
        items[current].classList.add('active');
        if (dots[current]) dots[current].classList.add('active');
    }

    function startTimer() {
        clearInterval(timer);
        timer = setInterval(() => { if (isMobile()) goTo(current + 1); }, 5000);
    }

    dots.forEach((dot, i) => {
        dot.addEventListener('click', () => { goTo(i); startTimer(); });
    });

    startTimer();
})();

// ── Canvas Particles / Star Field ───────────────────────────────────────────
(function () {
    const canvas = document.getElementById('particles-canvas');
    if (!canvas) return;
    const ctx = canvas.getContext('2d');

    function isMobile() { return window.innerWidth <= 768; }

    function resize() {
        canvas.width  = canvas.parentElement.offsetWidth;
        canvas.height = canvas.parentElement.offsetHeight;
    }
    resize();
    window.addEventListener('resize', resize);

    /* ── 별 생성 ── */
    function makeStar() {
        const mobile = isMobile();
        return {
            x:            Math.random() * canvas.width,
            y:            Math.random() * canvas.height,
            r:            mobile ? Math.random() * 1.6 + 0.3 : Math.random() * 1.4 + 0.3,
            baseAlpha:    Math.random() * 0.65 + 0.2,
            alpha:        0,
            phase:        Math.random() * Math.PI * 2,
            twinkleSpeed: Math.random() * 0.025 + 0.006,
            vx:           (Math.random() - 0.5) * (mobile ? 0.08 : 0.3),
            vy:           -(Math.random() * (mobile ? 0.12 : 0.4) + 0.05),
        };
    }

    let stars = [];
    function initStars() {
        stars = Array.from({ length: isMobile() ? 150 : 65 }, makeStar);
    }
    initStars();
    window.addEventListener('resize', initStars);

    /* ── 유성 (모바일 전용) ── */
    const meteors = [];
    let lastMeteor = 0;

    function makeMeteor() {
        const startX = Math.random() * canvas.width * 0.8;
        const startY = Math.random() * canvas.height * 0.4;
        return {
            x: startX, y: startY,
            len:    Math.random() * 100 + 60,
            speed:  Math.random() * 5 + 4,
            angle:  Math.PI / 4 + (Math.random() - 0.5) * 0.4,
            prog:   0,
            alpha:  1,
        };
    }

    /* ── 기포/원형 파티클 (데스크탑용) ── */
    function drawDesktop() {
        stars.forEach(p => {
            ctx.beginPath();
            ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
            ctx.fillStyle = `rgba(180, 210, 255, ${p.baseAlpha})`;
            ctx.fill();
            p.x += p.vx;
            p.y += p.vy;
            if (p.y < -6) { p.y = canvas.height + 6; p.x = Math.random() * canvas.width; }
            if (p.x < -6)  p.x = canvas.width + 6;
            if (p.x > canvas.width + 6) p.x = -6;
        });
    }

    /* ── 별 + 유성 (모바일용) ── */
    function drawMobile(now) {
        /* 별 반짝임 */
        stars.forEach(s => {
            s.phase += s.twinkleSpeed;
            s.alpha  = s.baseAlpha * (0.45 + 0.55 * Math.sin(s.phase));
            ctx.beginPath();
            ctx.arc(s.x, s.y, s.r, 0, Math.PI * 2);
            ctx.fillStyle = `rgba(210, 228, 255, ${s.alpha})`;
            ctx.fill();
            s.x += s.vx;
            s.y += s.vy;
            if (s.y < -6) { s.y = canvas.height + 6; s.x = Math.random() * canvas.width; }
            if (s.x < -6)  s.x = canvas.width + 6;
            if (s.x > canvas.width + 6) s.x = -6;
        });

        /* 유성 생성 (3~6초 간격) */
        if (now - lastMeteor > 3000 + Math.random() * 3000) {
            meteors.push(makeMeteor());
            lastMeteor = now;
        }

        /* 유성 그리기 */
        for (let i = meteors.length - 1; i >= 0; i--) {
            const m = meteors[i];
            m.prog += m.speed;
            m.alpha = Math.max(0, 1 - m.prog / (m.len * 1.8));

            const dx = Math.cos(m.angle) * m.prog;
            const dy = Math.sin(m.angle) * m.prog;

            const g = ctx.createLinearGradient(m.x, m.y, m.x + dx, m.y + dy);
            g.addColorStop(0, `rgba(255,255,255,0)`);
            g.addColorStop(1, `rgba(255,255,255,${m.alpha * 0.9})`);

            ctx.beginPath();
            ctx.moveTo(m.x, m.y);
            ctx.lineTo(m.x + dx, m.y + dy);
            ctx.strokeStyle = g;
            ctx.lineWidth = 1.8;
            ctx.stroke();

            /* 머리 글로우 */
            ctx.beginPath();
            ctx.arc(m.x + dx, m.y + dy, 1.5, 0, Math.PI * 2);
            ctx.fillStyle = `rgba(255,255,255,${m.alpha})`;
            ctx.fill();

            if (m.alpha <= 0) meteors.splice(i, 1);
        }
    }

    function draw(now) {
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        if (isMobile()) {
            drawMobile(now);
        } else {
            drawDesktop();
        }
        requestAnimationFrame(draw);
    }
    requestAnimationFrame(draw);
})();

// ── Product Lightbox ────────────────────────────────────────────────────────
(function () {
    const lightbox = document.getElementById('lightbox');
    const lbImg = document.getElementById('lightbox-img');
    if (!lightbox || !lbImg) return;

    const items = document.querySelectorAll('.gallery-item');
    const srcs = Array.from(items).map(el => el.dataset.src);
    let current = 0;

    function open(idx) {
        current = idx;
        lbImg.src = srcs[current];
        lightbox.classList.add('open');
        document.body.style.overflow = 'hidden';
    }

    function close() {
        lightbox.classList.remove('open');
        document.body.style.overflow = '';
    }

    function prev() { current = (current - 1 + srcs.length) % srcs.length; lbImg.src = srcs[current]; }
    function next() { current = (current + 1) % srcs.length; lbImg.src = srcs[current]; }

    items.forEach((item, i) => item.addEventListener('click', () => open(i)));
    document.querySelector('.lightbox-close').addEventListener('click', close);
    document.querySelector('.lightbox-prev').addEventListener('click', prev);
    document.querySelector('.lightbox-next').addEventListener('click', next);
    lightbox.addEventListener('click', e => { if (e.target === lightbox) close(); });
    document.addEventListener('keydown', e => {
        if (!lightbox.classList.contains('open')) return;
        if (e.key === 'Escape') close();
        if (e.key === 'ArrowLeft') prev();
        if (e.key === 'ArrowRight') next();
    });
})();

// ── Mobile Nav Menu ─────────────────────────────────────────────────────────
(function () {
    const btn = document.querySelector('.mobile-menu-btn');
    const nav = document.querySelector('.nav');
    if (!btn || !nav) return;

    btn.addEventListener('click', () => {
        nav.classList.toggle('nav-open');
        btn.classList.toggle('open');
    });

    document.querySelectorAll('.nav-list a').forEach(link => {
        link.addEventListener('click', () => {
            nav.classList.remove('nav-open');
            btn.classList.remove('open');
        });
    });
})();

// 폼 위에 성공/실패 메시지 표시
function showMessage(type, text) {
    const existing = document.getElementById('form-message');
    if (existing) existing.remove();

    const msg = document.createElement('div');
    msg.id = 'form-message';
    msg.style.cssText = `
        padding: 14px 20px;
        margin-bottom: 20px;
        border-radius: 6px;
        font-weight: 500;
        font-size: 15px;
        background: ${type === 'success' ? '#d4edda' : '#f8d7da'};
        color:      ${type === 'success' ? '#155724' : '#721c24'};
        border:     1px solid ${type === 'success' ? '#c3e6cb' : '#f5c6cb'};
    `;
    msg.textContent = text;

    const form = document.querySelector('.contact-form');
    form.insertAdjacentElement('beforebegin', msg);
    msg.scrollIntoView({ behavior: 'smooth', block: 'center' });

    setTimeout(() => msg.remove(), 6000);
}
